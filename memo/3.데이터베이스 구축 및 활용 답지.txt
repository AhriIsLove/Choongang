문항 4 :    -5
      150
    [풀이]
    SUM(컬럼) 의 경우, NULLABLE 컬럼에서 NULL 값이 있다면 무시하고 NULL이 아닌 값을 더한 값을 보여준다.
    즉,
    SUM(COL1) = 30 + 50 = 80,
    SUM(COL2) = 10,
    SUM(COL3) = 20 + 40 = 60
    위 세 값을 모두 더하는 질의문이므로, 그 결과
    150 이 출력된다.